import { Context } from "@opentelemetry/api";
import { SpanProcessor, Span } from "@opentelemetry/sdk-trace-base";
export declare class PaidSpanProcessor implements SpanProcessor {
    private static readonly SPAN_NAME_PREFIX;
    private static readonly PROMPT_ATTRIBUTES_SUBSTRINGS;
    onStart(span: Span, _parentContext?: Context): void;
    onEnd(): void;
    shutdown(): Promise<void>;
    forceFlush(): Promise<void>;
}
